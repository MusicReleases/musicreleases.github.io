@using JakubKastner.MusicReleases.Enums

@inject NavigationManager NavManager

<div class="logo @CssClass" @onclick="LogoOnClick">
    @if (DisplayMusic)
    {
        <span>Music</span>
    }
    <MrIcon Icon="CustomIcon.Logo" Size="1em" />@* 
     *@<span>Releases</span>
</div>
@using JakubKastner.MusicReleases.Spotify.Tasks


<div class="tasks">

    <div class="header">
        <MrButton Class="sidebar-title tasks" Text="Active tasks" OnClick="@ViewTasks">
            <MrIcon Icon="@LucideIcon.ListTodo" />
        </MrButton>

        <MrButton Class="sidebar-header" Title="View all tasks" OnClick="@ViewTasks">
            <MrIcon Icon="@LucideIcon.ListCheck" />
        </MrButton>

        @if (BackgroundTaskManager.AnyTaskVisible)
        {
            <MrButton Class="sidebar-header" Title="Close finished tasks" OnClick="@BackgroundTaskManager.HideAllEnded">
                <MrIcon Icon="@LucideIcon.X" />
            </MrButton>
        }
    </div>

    <div class="list scroll">

        @if (!BackgroundTaskManager.AnyTaskVisible)
        {
            <p>No active tasks</p>
        }
        else
        {
            <Virtualize Items="@BackgroundTasks" Context="task" TItem="@BackgroundTask">
                <BackgroundTaskCard BackgroundTask="@task" @key="task.Id" />
            </Virtualize>
        }
    </div>
</div>
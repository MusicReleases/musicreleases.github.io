<div class="@TaskClass" @onclick="ViewTask">

    <div class="header">

        <span class="title">
            @SpotifyBackgroundTask.Name
        </span>

        <div class="icon">
            @if (SpotifyBackgroundTask.Ended)
            {
                <MrButton Class="task" OnClick="@HideTask" Title="Hide task">
                    <MrIcon Icon="@LucideIcon.X" />
                </MrButton>
            }
            else
            {
                <MrIcon Icon="LucideIcon.LoaderCircle" Spin="@SpotifyBackgroundTask.IsRunning" />
            }
        </div>

    </div>

    <div class="status">

        <span>
            @SpotifyBackgroundTask.StatusText
        </span>

        @if (SpotifyBackgroundTask.CurrentStep is not null)
        {
            <span>
                &nbsp;(@SpotifyBackgroundTask.CurrentStep.Name)
            </span>
        }

    </div>

</div>
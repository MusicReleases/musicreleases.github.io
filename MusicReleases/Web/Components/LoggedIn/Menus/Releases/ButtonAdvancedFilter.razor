@using JakubKastner.MusicReleases.Services.BaseServices

@implements IDisposable

@inject ISpotifyFilterUrlService SpotifyFilterUrlService
@inject ISpotifyFilterService SpotifyFilterService
@inject NavigationManager NavManager

<MrButton Class="@ButtonClass(LeftType, "left")" Title="@ButtonTitle(LeftType, LeftTitle)" Text="@LeftText" @onclick="() => ChangeFilter(LeftType)" />
<MrButton Class="@ButtonClass(RightType, "right")" Title="@ButtonTitle(RightType, RightTitle)" Text="@RightText" @onclick="() => ChangeFilter(RightType)" />
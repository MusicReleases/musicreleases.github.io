@using JakubKastner.SpotifyApi.SpotifyEnums

<ul class="@ListClass">

    @foreach (var releaseType in EnumReleaseTypeExtensions.GetAllReleaseTypes())
    {
        if (releaseType == ReleaseType.Podcasts)
        {
            // TODO podcasts
            continue;
        }
        <li class="@releaseType.ToLowerString()">
            <ReleaseMenuButton ReleaseType=@releaseType Class="@ReleaseButtonClass" />
        </li>
    }

    @if (MenuType == ReleaseMenuComponent.Primary)
    {
        <li class="@OverflowListItemClass">
            <MrButton Class="@OverflowButtonClass" Text="More" Title="@OverflowButtonTitle" OnClick="@ToggleOverflowMenu" aria-expanded="@IsOverflowMenuDisplayed" aria-controls="more-releases">
                <MrChip Class="@ClassActiveOverflow">
                    <MrIcon Icon="@OverflowIcon" />
                </MrChip>
            </MrButton>
        </li>
    }

</ul>
@* header *@
<header class="@TypeString-header">
    @Header
</header>

@* filter *@
<search class="@TypeString-filter" role="search">
    @if (Loading)
    {
        <p>Loading...</p>
    }
    else
    {
        @Filter
    }
</search>

@* content (list) *@
<nav class="@TypeString-menu" aria-label="@ContentAriaLabel">
    @if (Loading)
    {
        <p>Loading...</p>
    }
    else
    {
        @ChildContent
    }
</nav>
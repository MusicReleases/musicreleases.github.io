@* header *@
<header class="@TypeString-header header" style="grid-area: @TypeString-header">
    @Header
</header>

@* filter *@
<search class="@TypeString-filter filter" style="grid-area: @TypeString-filter" role="search">
    @if (Loading)
    {
        <p>Loading...</p>
    }
    else
    {
        @Filter
    }
</search>

@* content (list) *@
<nav class="@TypeString-menu menu scroll" style="grid-area: @TypeString-menu" aria-label="@ContentAriaLabel">
    @if (Loading)
    {
        <p>Loading...</p>
    }
    else
    {
        @ChildContent
    }
</nav>
@using JakubKastner.MusicReleases.Enums
@using JakubKastner.MusicReleases.Services.ApiServices
@using JakubKastner.MusicReleases.Services.UiServices
@using JakubKastner.MusicReleases.Web.Components.LoggedIn.Footers
@using JakubKastner.MusicReleases.Web.Components.LoggedIn.Headers
@using JakubKastner.MusicReleases.Web.Components.LoggedIn.Menus.Main

@inherits LayoutComponentBase;

@implements IDisposable

@inject IMobileService MobileService
@inject IApiLoginService ApiLoginService
@inject NavigationManager NavManager

@* main layout - user is sucessfully logged in *@

<div class="body">

    @if (!ApiLoginService.IsUserLoggedIn())
    {
        <p>Error - User is not logged in</p>
    }
    else
    {
        <PrimaryHeader />
        <SecondaryHeader />

        @* menu *@
        <MenuMain />
        <MenuDateYear Class="@MobileService.GetMenuClass(DisplayMobile.Date)" />
        <MenuArtists Class="@MobileService.GetMenuClass(DisplayMobile.Artists)" />
        <MenuPlaylists />

        <div class="main">
            <div class="content page scroll @MobileService.GetMenuClass(DisplayMobile.Releases)">
                @Body
            </div>

        </div>

        <Footer />
    }

</div>